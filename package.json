{
  "name": "stocktracker",
  "version": "1.0.0",
  "description": "A TypeScript project that dynamically calculates and returns real-time stock levels for SKUs based on transactions and initial stock data. Ensures accurate tracking and management of inventory, with error handling for missing SKUs.",
  "main": "index.js",
  "scripts": {
    "dev": "tsc && node dist/index.js",
    "test": "tsc && node dist/test.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^20.10.8",
    "fs": "^0.0.1-security",
    "typescript": "^5.3.3"
  }
}
